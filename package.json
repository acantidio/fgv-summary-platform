{
  "name": "fgv-summary-platform",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "render": "op run --env-file=.env.tpl -- node render.js",
    "test": "node --test test.js",
    "enrich": "op run --env-file=.env.tpl -- node enrich.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
